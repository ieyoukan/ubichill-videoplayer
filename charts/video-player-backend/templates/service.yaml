apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-player-backend.fullname" . }}
  labels:
    {{- include "video-player-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "video-player-backend.selectorLabels" . | nindent 4 }}
